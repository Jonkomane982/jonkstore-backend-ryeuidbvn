# Core Errors
This folder contains standardized Failure classes used across the domain layer to represent business-logic errors.
- `failures.dart`: Base failure class and common implementations like `ServerFailure`.
